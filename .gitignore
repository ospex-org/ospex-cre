# Secrets — never commit
*.env
.env

# Operator-filled target configs — copy from the committed config.*.example.json templates
# and fill in the deploy-specific addresses. The real configs are never committed.
oracle/config.staging.json
oracle/config.production.json

# Dependencies
node_modules/

# CRE build artifacts (regenerate with `cre workflow build`)
.cre_build_tmp.js
binary.wasm
binary.wasm.br.b64
dist/
